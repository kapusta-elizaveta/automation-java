package pages.billing;

import org.openqa.selenium.WebDriver;
import pages.AbstractPage;

public class NewCardPage extends AbstractPage {
    protected NewCardPage(WebDriver driver) {
        super(driver);
    }
}
